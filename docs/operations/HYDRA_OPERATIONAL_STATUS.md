# HYDRA_OPERATIONAL_STATUS

Status source: current command state
Mode: HYDRA_HOME canonical execution

## LOCKED

- Rust + .NET + WiX as canonical implementation/deployment stack.
- Expo mobile as mobile HUD surface.
- Figma as master design file.
- Curated asset pipeline instead of uncontrolled full bundle.
- Voice extracted from monolithic `App.tsx` into dedicated runtime/contracts/services.
- Python/FastAPI/Inno rejected.

## ACTIVE

- Tooling intelligence pack.
- Implementation execution plan.
- Figma-to-code map.
- Asset curation spec.
- Repository and ZIP structure debugging.

## CONFLICT

- Historical Python/FastAPI traces in `Build_AI` or previous snapshots.
- Legacy voice assumptions embedded in old monolithic `App.tsx`.
- No single final executable repo snapshot yet.

## Operating rule

Conflicts are resolved architecturally. They do not reopen forbidden technologies.
